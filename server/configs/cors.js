const corsOptions = {
    credentials: true,
    origin: ['https://fullstack-test-deploy.vercel.app', 'https://fullstack-test-deploy-api.vercel.app'] // Whitelist the domains you want to allow
};

export default corsOptions
